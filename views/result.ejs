<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Twitter Trends</title>
    <!-- <link rel="stylesheet" href="/public/css/styles.css"> -->
<style>
    /* General Styles */
    /* General Styles */
    body {
    margin: 0;
    padding: 0;
    font-family: Arial, sans-serif;
    background: linear-gradient(135deg, #555555, #252525);
    color: #fff;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

.container {
    text-align: center;
    background: rgb(0, 0, 0);
    border-radius: 10px;
    padding: 20px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    max-width: 500px;
    width: 90%;
}

/* Headings and Text */
h1 {
    font-size: 2rem;
    margin-bottom: 20px;
}

p {
    font-size: 1.2rem;
    margin-bottom: 20px;
    color: #e0e0e0;
}

.ip-address {
    font-weight: bold;
    color: #ffcc00;
}

/* Button Styles */
button.action-button {
    background: #ffcc00;
    border: none;
    padding: 10px 20px;
    font-size: 1rem;
    font-weight: bold;
    border-radius: 5px;
    color: #333;
    cursor: pointer;
    transition: background 0.3s, transform 0.2s;
}

button.action-button:hover {
    background: #e6b800;
    transform: scale(1.05);
}

/* Back Button */
a.back-button {
    display: inline-block;
    margin-top: 20px;
    text-decoration: none;
    color: #ffcc00;
    font-weight: bold;
    transition: color 0.3s;
}

a.back-button:hover {
    color: #e6b800;
}

/* Trends List */
ul.trends-list {
    list-style: none;
    padding: 0;
}

li.trend-item {
    background: rgba(255, 255, 255, 0.2);
    padding: 10px;
    margin: 5px 0;
    border-radius: 5px;
    font-size: 1rem;
    color: #fff;
    text-align: left;
}
</style>
</head>
<body>
    <div class="container">
        <h1>Twitter Trends</h1>
        <p>These are the most happening topics as on <%= timestamp %>:</p>
        <ul class="trends-list">
            <% trends.forEach(trend => { %>
                <li class="trend-item"><%= trend %></li>
            <% }); %>
        </ul>
        <!-- <p>The IP address used for this query was <span class="ip-address"><%= ipAddress %></span>.</p> -->
        <a href="/" class="back-button">Go back</a>
    </div>
</body>
</html>
